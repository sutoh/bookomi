<% if @error.present? %>
  <div id="error_explanation">
    <%= @error %>
  </div>
<% end %>
<%= @tag.try(:name) %>
<ul>
  <% @tag.mangas.each do |manga| %>
    <li><%= link_to manga.tweet.tweet_id, tweet_path(manga.tweet.id) %></li>
  <% end %>
</ul>
