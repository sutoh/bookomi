<%= form_for(@tweet) do |f| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="tw-container">
  <div class="container">
    <div class="field form-group">
      <%= f.label :tweet_id, "URL" %><br>
      <%= f.text_field :tweet_id, :class => "form-control" %>
    </div>
  </div>
  <div class="actions">
      <%= f.submit :class => "btn btn-primary", :value => "投稿する" %>
  </div>
</div>

<% end %>


<div class="tw-container">
  使い方
  <br><br>
  ①面白い画像が投稿されているツイートの詳細を表示
  <br><br>
  <img src='/assets/how_to_use1.png'>
  <br><br>
  ②URLをコピー
  <br><br>
  <img src='/assets/how_to_use2.png'>
  <br><br>
  ③コピーしたURLを↓に貼り付け
  <br><br>
</div>

  <%= form_for(@tweet) do |f| %>
    <% if @tweet.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

        <ul>
        <% @tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


  <div class="tw-container">
    <div class="container">
      <div class="field form-group">
        <%= f.label :tweet_id, "URL" %><br>
        <%= f.text_field :tweet_id, :class => "form-control" %>
      </div>
    </div>
    <div class="actions">
        <%= f.submit :class => "btn btn-primary", :value => "投稿する" %>
    </div>
  </div>
<% end %>
